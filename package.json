"scripts": {
    "build": "mkdir -p public && cp index.html public/index.html && mkdir -p public/assets/js && cp asset-loader.js public/assets/js/asset-loader.js && echo 'Build complete'",
    "deploy": "wasmer deploy"
  },
